<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8"/>
    <title><%= locals.title ? title : "Event Management" %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-900 min-h-screen flex flex-col">

<header class="bg-blue-700 text-white py-4 px-6 flex justify-between items-center">
    <a href="/" class="flex items-center space-x-3">
        <img src="/img/logo.png" alt="Logo" class="h-10"/>
        <span class="font-bold text-xl">Event Management</span>
    </a>
    <% if (locals.user) { %>
        <div>
            <span class="mr-4">Salut, <%= user.name %>!</span>
            <a href="/dashboard" class="bg-green-600 px-3 py-1 rounded hover:bg-green-700">Dashboard</a>
            <form action="/logout" method="POST" class="inline">
                <button type="submit" class="bg-red-600 px-3 py-1 rounded hover:bg-red-700">
                    Deconectare
                </button>
            </form>
        </div>
    <% } else { %>
        <a href="/login" class="bg-white text-blue-700 px-4 py-2 rounded hover:bg-gray-200">Log In</a>
    <% } %>
</header>

<main class="flex-grow max-w-5xl mx-auto p-6 w-full">
    <%- body %>
</main>

<footer class="bg-gray-200 text-center py-4 mt-6">
    <p class="text-gray-600 text-sm">&copy; <%= new Date().getFullYear() %> Event Management. Toate drepturile
        rezervate.</p>
</footer>

</body>
</html>